import XCTest
@testable import rides_ios_sdk_spm

final class rides_ios_sdk_spmTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(rides_ios_sdk_spm().text, "Hello, World!")
    }
}
